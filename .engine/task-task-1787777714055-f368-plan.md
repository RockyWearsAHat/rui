# Engine Project Plan

Outer iterations: 501 / 200

## Steps

- [x] **1. Establish pre-commit git hook for format enforcement** (attempts: 1)
  - Add a test in `tests/setup.rs` that verifies `.git/hooks/pre-commit` exists and is executable. Implement by writing a bash pre-commit hook that runs `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` before allowing commits. Refactor by documenting the hook contract in `.helpers/hooks.md`.
  - **Acceptance:** `bash /Users/alexwaldmann/Desktop/rui/.git/hooks/pre-commit` succeeds with exit code 0 when code is formatted and lints pass.

- [x] **2. Verify Rust toolchain and dependencies availability** (attempts: 2)
  - Add a test in `tests/setup.rs` that checks `rustc --version` and `cargo --version` are callable and meet minimum requirements (1.85+). Implement by adding version checks. Refactor by documenting required Rust version in CLAUDE.md.
  - **Acceptance:** `rustc --version` outputs version >= 1.85 and `cargo --version` succeeds.

- [x] **3. Test build from clean state** (attempts: 5)
  - Add a test in `tests/setup.rs` that runs `rm -rf target && cargo build --all-targets`. Implement the build step. Refactor by caching build artifacts between test runs.
  - **Acceptance:** `cargo build --all-targets` completes with 0 exit code in under 60 seconds on an empty target directory.

- [x] **4. Verify all library and integration tests pass** (attempts: 5)
  - Add a test in `tests/setup.rs` that runs `cargo test --all-targets --lib` and `cargo test --test '*'`. Implement by executing the test commands. Refactor by capturing and displaying test output on failure.
  - **Acceptance:** `cargo test --all-targets` exits with 0 and shows "test result: ok" with all tests passing.

- [x] **5. Verify documentation builds without warnings** (attempts: 5)
  - Add a test in `tests/setup.rs` that runs `cargo doc --no-deps` with `RUSTDOCFLAGS=-D warnings`. Implement by setting the env var and building docs. Refactor by suppressing benign doc warnings.
  - **Acceptance:** `cargo doc --no-deps` exits with 0 and produces `target/doc/rui/index.html`.

- [x] **6. Verify examples compile and identify entry points** (attempts: 5)
  - Add a test in `tests/setup.rs` that enumerates `examples/*.rs` files and verifies each compiles. Implement by running `cargo build --examples` and checking each binary exists in target/debug/examples/. Refactor by documenting each example's purpose in a comment block in index.dx.
  - **Acceptance:** `cargo build --examples` exits with 0 and `ls target/debug/examples/ | grep -E '^(counter|controls|gallery)$'` lists all three binaries.

- [x] **7. Document project structure and entry points in index.dx** (attempts: 5)
  - Modify `index.dx` to list the main modules (`element`, `shell`, `paint`, `canvas`), their responsibilities, and the location of the library entry point (`src/lib.rs`). Verify by running `dx_read index.dx` that all sections render correctly. Refactor by ensuring the document cross-links to README.md.
  - **Acceptance:** `cat index.dx` shows a dx1 header and `dx read index.dx` displays the structure without errors.

- [x] **8. End-to-end local verification: build, test, lint, format** (attempts: 5)
  - Add a test in `tests/setup.rs` that runs the full quality pipeline: `cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --all-targets && cargo build --examples`. Implement as a single assertion that all four commands exit with 0. Refactor by parallelizing independent checks where possible.
  - **Acceptance:** Running `cargo test --test setup --lib` passes and reports the pipeline succeeded.

- [x] **9. Verify Cargo.toml is release-ready and document build/test commands** (attempts: 5)
  - Review `Cargo.toml` for correct version, description, repository, and license fields. Add a section to CLAUDE.md listing exact commands: `cargo build`, `cargo test`, `cargo fmt`, `cargo clippy`. Implement by reading and validating Cargo.toml fields. Refactor by auto-generating a build checklist in `.engine/build-checklist.txt`.
  - **Acceptance:** `cargo metadata --format-version 1 | jq '.packages[0] | {name, version, description}' | grep -q '"rui"'` shows v0.1.0 with a description.

- [ ] **10. Publish to crates.io and record live artifact URL** (attempts: 5)
  - Add a test in `tests/setup.rs` that verifies `cargo publish --dry-run` succeeds (no actual upload). Implement by running the dry-run publish. After all tests pass, execute `cargo publish` to publish v0.1.0 to crates.io. Refactor by writing the public crates.io URL to `.engine/live-url.txt` in the format `https://crates.io/crates/rui/0.1.0`.
  - **Acceptance:** `curl -sf https://crates.io/api/v1/crates/rui/0.1.0 | jq '.crate.name'` returns "rui" and `.engine/live-url.txt` contains the URL.
  - **Last feedback:** Behavioral validation failed: create validate session: attempt to write a readonly database (1032)

