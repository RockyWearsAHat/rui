# What the compiler makes.
/target
Cargo.lock

# What the examples write when they are asked to draw with no window open.
*.png

# Internal build/cache artifacts and framework state
.cache/
.doc/
.engine/
.claude/
# dx: the local index is rebuildable from .doc/repo.dxcp
.doc/index.db
.doc/index.db-wal
.doc/index.db-shm
.doc/coverage.jsonl
.doc/local.dxcp
